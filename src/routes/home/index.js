import { h } from 'preact';
import style from './style';

const Home = () => (
  <div class={style.home}>
    <h1>Trang chủ</h1>
    <p>Đây là thành phần Home.compoment</p>
  </div>
);

export default Home;
